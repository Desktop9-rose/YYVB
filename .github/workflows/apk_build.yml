name: Build APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Free Disk Space
        uses: jlumbroso/free-disk-space@v1.3.1
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: Build with Buildozer
        uses: ArtemSBulgakov/buildozer-action@master  # 核心修改：改用master分支（修复了镜像名bug）
        id: buildozer
        with:
          command: buildozer android debug
          buildozer_version: master
          # 可选启用缓存（加速后续构建）
          cache: true

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: package
          path: bin/*.apk
          retention-days: 3
